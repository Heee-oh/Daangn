spring:
  application:
    name: market

  jackson:
    property-naming-strategy: SNAKE_CASE

  datasource:
    url: jdbc:postgresql://localhost:5433/daangn
    username: user
    password: 1234
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: false
    open-in-view: false
    hibernate:
      ddl-auto: update

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.daangn.market.Listing.application.dto, com.daangn.market.member
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false

jwt:
  secret: seg$#%3452FGFRdsrgd@#$44ZA4343Fh45346
  access-token-validity-seconds: 360024100
